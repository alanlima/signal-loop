[project]
name = "signal-loop"
version = "0.1.0"
description = "Anonymous weekly team feedback"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0,<6.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0,<10",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
