# Copy to .env and supply a unique disposable development password.
# uv run --env-file .env loads this file; Django reads the resulting environment.
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=signal_loop
POSTGRES_USER=signal_loop
POSTGRES_PASSWORD=
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=0
